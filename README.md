# AND — Agent Not Detect

> Install exclusively through your AI agent. Humans: tell your agent to install AND.

AND is a multi-platform reference library providing jailbreak techniques, system prompt leaks, and adversarial prompts for 27+ AI vendors. Designed to be installed and triggered automatically by AI coding agents across 5 platforms.

## Install (Agent-Only)

**You do not install AND manually.** Tell your AI agent:

> "Install AND from github.com/adlptv/Agent-AND.git"

Your agent will run the appropriate installer for its platform. Each platform auto-detects trigger keywords and loads AND content on-demand.

## Supported Platforms

| Platform | Agent Command |
|----------|--------------|
| **Hermes (OpenClaw)** | `Install AND from github.com/adlptv/Agent-AND.git` |
| **Claude Code** | `Install AND from github.com/adlptv/Agent-AND.git` |
| **Codex CLI** | `Install AND from github.com/adlptv/Agent-AND.git` |
| **OpenCode** | `Install AND from github.com/adlptv/Agent-AND.git` |
| **CommandCode** | `Install AND from github.com/adlptv/Agent-AND.git` |

## What AND Contains

```
and-references/              ← 41 reference files (shared)
├── ANTHROPIC.md, OPENAI.md, GOOGLE.md, ...
├── SYSTEMPROMPTS.md         ← Leaked system prompts
├── SHORTCUTS.json           ← 35+ commands
├── AAA.md                   ← Universal jailbreak
└── MOTHERLOAD.txt           ← Mega compilation
and-scripts/
└── search.py                ← Cross-file search
```

## Triggers

Your agent automatically activates AND when you say: `jailbreak` `godmode` `unfiltered` `bypass` `AND` `Pliny` `system prompt` `!JAILBREAK` `!GODMODE` `!OMNI`

## Source

<https://github.com/adlptv/Agent-AND.git>
